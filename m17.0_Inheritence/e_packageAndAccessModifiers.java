public class e_packageAndAccessModifiers {
  public static void main(String[] args) {

  }
}
/* Package */
// A package in Java is used to group related classes.
// Think of it as a folder in a file directory.
// We use packages to avoid name conflicts, and to write a better maintainable
// code.

// Packages are divided into two categories:
// 1.Built-in Packages (packages from the Java API)
// 2.User-defined Packages (create your own packages)

/* Access Modifiers */
// in java, Access Modifiers are Public, Protected, Default, Private.
// in java, we can apply Access Modifiers in class, Variable, Method.

/*-------------Access of Access Modifier---------- */
/*
 * public :with same class || outside class same package || outside package
 * Subclass || outside package non-subClass
 */
/*
 * protected :with same class || outside class same package || outside package
 * Subclass
 */
/*
 * default :with same class || outside class same package.
 */
/*
 * private :with same class
 */